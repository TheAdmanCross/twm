# Content Folder

Content in this folder is intended to be browser-editable through Decap CMS.

- `settings/home.json`: main homepage content model
- `testimonials/*.json`: one file per testimonial so wins can be added without touching code
